<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VSM Tuyển Dụng</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
    <nav class="bg-white shadow-md">
        <div class="container mx-auto px-6 py-3 flex justify-between items-center">
            <a class="text-xl font-bold text-gray-800" href="/">VSM Recruit</a>
            <div>
                <a class="text-gray-800 hover:text-blue-500 mx-2" href="/jobs">Việc Làm</a>
                <% if (user) { %>
                    <% if (user.role === 'admin') { %>
                        <a class="text-gray-800 hover:text-blue-500 mx-2" href="/admin/dashboard">Admin Dashboard</a>
                    <% } %>
                    <span class="text-gray-600 mx-2">Chào, <%= user.full_name %>!</span>
                    <a class="bg-red-500 text-white py-2 px-4 rounded hover:bg-red-600 mx-2" href="/auth/logout">Đăng Xuất</a>
                <% } else { %>
                    <a class="text-gray-800 hover:text-blue-500 mx-2" href="/auth/login">Đăng Nhập</a>
                    <a class="bg-blue-500 text-white py-2 px-4 rounded hover:bg-blue-600 mx-2" href="/auth/register">Đăng Ký</a>
                <% } %>
            </div>
        </div>
    </nav>
    <main class="container mx-auto px-6 py-8">